
  # Create UI with Font Style

  This is a code bundle for Create UI with Font Style. The original project is available at https://www.figma.com/design/57hIzaOtJSDPFcXHl7fsYl/Create-UI-with-Font-Style.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  